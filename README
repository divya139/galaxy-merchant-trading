
The purpose of this file is to give an overview of how to deploy this project.

Pre-requisites:
In order to deploy this project we need NodeJS (latest version) and npm to be installed on the server.


Deployment Instructions:
1. Type following command to download the dependencies for this project.
    'npm init'
2. To start the backend server, type the following command,
    'node APIConsumer.js'
3. Open Web browser (prefereably Google Chrome) at, http://localhost:3000